CREATE TABLE FakeFootChampion (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name_player VARCHAR(255) NOT NULL,
    team VARCHAR(255) NOT NULL,
    position VARCHAR(100) NOT NULL,
    rating INT NOT NULL
);
